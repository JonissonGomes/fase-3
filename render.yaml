databases:
  - name: revenda-mongodb
    databaseName: revenda_veiculos
    user: revenda_user

services:
  # Auth Service
  - type: web
    name: fase-3-auth-service
    env: node
    plan: free
    buildCommand: npm install --workspace=services/auth-service --only=production && npm list --workspace=services/auth-service
    startCommand: npm start --workspace=services/auth-service
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        generateValue: true
      - key: MONGODB_URI
        fromDatabase:
          name: revenda-mongodb
          property: connectionString
      - key: FRONTEND_URL
        value: https://fase-3.vercel.app
      - key: LOG_LEVEL
        value: info

  # Vehicles Service
  - type: web
    name: fase-3-vehicles-service
    env: node
    plan: free
    buildCommand: npm install --workspace=services/vehicles-service --only=production
    startCommand: npm start --workspace=services/vehicles-service
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        fromService:
          type: web
          name: fase-3-auth-service
          envVarKey: JWT_SECRET
      - key: MONGODB_URI
        fromDatabase:
          name: revenda-mongodb
          property: connectionString
      - key: FRONTEND_URL
        value: https://fase-3.vercel.app
      - key: LOG_LEVEL
        value: info

  # Orders Service
  - type: web
    name: fase-3-orders-service
    env: node
    plan: free
    buildCommand: npm install --workspace=services/orders-service --only=production
    startCommand: npm start --workspace=services/orders-service
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: JWT_SECRET
        fromService:
          type: web
          name: fase-3-auth-service
          envVarKey: JWT_SECRET
      - key: MONGODB_URI
        fromDatabase:
          name: revenda-mongodb
          property: connectionString
      - key: VEHICLES_SERVICE_URL
        value: https://fase-3.onrender.com
      - key: FRONTEND_URL
        value: https://fase-3.vercel.app
      - key: LOG_LEVEL
        value: info 